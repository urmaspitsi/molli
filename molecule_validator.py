import numpy as np
from typing import Any, Dict, List, Tuple, Union

from ase import Atoms

import ase_utils as au
import features as ft


def validate_structure(mol: Atoms, constraints: List[Constraints]) -> bool:
  return True
